<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <!--
      3.5.16 y no 4.1.0: el escaneo de dependencias del CI encontro 38 vulnerabilidades conocidas
      (6 criticas, entre ellas CVE-2025-24813, una RCE en Tomcat) que entraban por las
      dependencias transitivas de 3.4.1. La rama 3.5 las trae parcheadas sin cambios de version
      mayor — Boot 4 arrastra Spring Framework 7 y no es un salto que hacer a la ligera.
    -->
    <version>3.5.16</version>
    <relativePath/>
  </parent>

  <groupId>com.aguavigia</groupId>
  <artifactId>ctg-backend</artifactId>
  <version>1.0.0</version>
  <name>ctg-backend</name>
  <description>AguaVigia CTG - backend</description>

  <properties>
    <java.version>21</java.version>
    <!--
      Spring Boot 3.5.16 fija netty 4.1.135, y CVE-2026-59901 (bucle infinito en el decodificador
      bzip2 de netty-codec-compression) se corrige en 4.1.136. Netty entra aqui como transitiva de
      Lettuce, el cliente de Redis, y este backend no descomprime bzip2 en ninguna parte — pero
      sobreescribir una version de parche cuesta una linea y deja el escaneo del CI en verde sin
      excepciones que alguien tenga que recordar despues.
    -->
    <netty.version>4.1.136.Final</netty.version>
    <!--
      Spring Boot 3.5.16 fija tomcat-embed 10.1.55, y el escaneo del CI encontro tres criticas:
      CVE-2026-65182 (bypass de restriccion de seguridad por control de acceso indebido),
      CVE-2026-65905 (bypass de autenticacion por repeticion en el autenticador DIGEST) y
      CVE-2026-68525 (acceso a recursos por bypass de la autenticacion FORM).

      Solo la primera afecta a este backend: DIGEST y FORM estan deshabilitados explicitamente en
      SecurityConfig, que autentica con JWT. Aun asi la primera toca justo lo que separa
      /api/veedor/** de lo publico, y subir una version de parche cuesta una linea.

      10.1.59 y no la 10.1.58 que nombra el aviso: esa version no llego a publicarse en Maven
      Central — la linea 10.1.x salta de la 57 a la 59. Comprobado contra maven-metadata.xml.
    -->
    <tomcat.version>10.1.59</tomcat.version>
    <mapstruct.version>1.6.3</mapstruct.version>
    <testcontainers.version>1.21.3</testcontainers.version>
    <jjwt.version>0.12.6</jjwt.version>
    <!--
      Docker Engine 29 subio su MinAPIVersion a 1.40, pero docker-java (dentro de Testcontainers
      1.21.3, la ultima publicada) sigue negociando 1.32 y el daemon responde 400 — el sintoma es
      "Could not find a valid Docker environment", que no dice nada del motivo real. Ver BUG-007.
      1.41 es la ventana mas ancha: la soporta Docker 20.10 en adelante y cumple el minimo de 29.
      Se quita cuando Testcontainers publique una version que negocie sola.
    -->
    <docker.api.version>1.41</docker.api.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.testcontainers</groupId>
        <artifactId>testcontainers-bom</artifactId>
        <version>${testcontainers.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-mongodb</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-redis</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>

    <!-- Envío de correo de M4 (D1, Sprint 1) — JavaMailSender contra Mailhog/SMTP real -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-mail</artifactId>
    </dependency>

    <!-- Mappers DTO <-> dominio (D3, Sprint 1) -->
    <dependency>
      <groupId>org.mapstruct</groupId>
      <artifactId>mapstruct</artifactId>
      <version>${mapstruct.version}</version>
    </dependency>

    <!-- Contrato OpenAPI publicado para D4 (D3, Sprint 1 - compuerta C2) -->
    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
      <version>2.8.6</version>
    </dependency>

    <!-- Circuit breakers y reintentos del pipeline de ingesta (D3, Sprint 3-4) -->
    <dependency>
      <groupId>io.github.resilience4j</groupId>
      <artifactId>resilience4j-spring-boot3</artifactId>
      <version>2.3.0</version>
    </dependency>

    <!-- JWT del panel del veedor (D3, Sprint 3 - RF019, RNF011). BCryptPasswordEncoder tambien
         viene de aqui (spring-security-crypto), para no depender de una libreria aparte. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-api</artifactId>
      <version>${jjwt.version}</version>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-impl</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-jackson</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <optional>true</optional>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.tngtech.archunit</groupId>
      <artifactId>archunit-junit5</artifactId>
      <version>1.3.0</version>
      <scope>test</scope>
    </dependency>

    <!-- Pruebas de integración de adaptadores Mongo/Redis (DoD de D3) -->
    <!-- Aporta @ServiceConnection: apunta Spring al contenedor sin @DynamicPropertySource -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-testcontainers</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>testcontainers</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>mongodb</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <excludes>
            <exclude>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
            </exclude>
          </excludes>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <annotationProcessorPaths>
            <path>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
            </path>
            <path>
              <groupId>org.mapstruct</groupId>
              <artifactId>mapstruct-processor</artifactId>
              <version>${mapstruct.version}</version>
            </path>
          </annotationProcessorPaths>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <systemPropertyVariables>
            <!-- docker-java lee `api.version`; sin esto las pruebas con Testcontainers no
                 encuentran Docker contra Engine 29. Ver la propiedad arriba y BUG-007. -->
            <api.version>${docker.api.version}</api.version>
          </systemPropertyVariables>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>0.8.12</version>
        <executions>
          <execution>
            <id>prepare-agent</id>
            <goals>
              <goal>prepare-agent</goal>
            </goals>
          </execution>
          <execution>
            <id>report</id>
            <phase>test</phase>
            <goals>
              <goal>report</goal>
            </goals>
          </execution>
          <execution>
            <id>check</id>
            <phase>verify</phase>
            <goals>
              <goal>check</goal>
            </goals>
            <configuration>
              <rules>
                <rule>
                  <element>PACKAGE</element>
                  <includes>
                    <include>com.aguavigia.ctg.domain.*</include>
                    <include>com.aguavigia.ctg.application.*</include>
                  </includes>
                  <limits>
                    <limit>
                      <counter>INSTRUCTION</counter>
                      <value>COVEREDRATIO</value>
                      <!-- RNF017 exige >= 70% en domain/ y application/. El umbral se fija en
                           85% y no en 70: real al momento de fijarlo, 92.4% en domain/ y 99.2%
                           en application/. Un umbral 20 puntos por debajo del valor real no
                           protege de nada — se podria borrar un tercio de las pruebas sin que
                           el build se enterara. El margen que queda sigue siendo suficiente para
                           no perseguir el 100%. -->
                      <minimum>0.85</minimum>
                    </limit>
                  </limits>
                </rule>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

</project>
