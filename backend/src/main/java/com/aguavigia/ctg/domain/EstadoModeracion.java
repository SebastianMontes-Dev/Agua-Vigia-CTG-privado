package com.aguavigia.ctg.domain;

/** RF018 (`ADR-023`) — todo reporte nace PENDIENTE y es candidato a moderación hasta que el veedor decida. */
public enum EstadoModeracion {
    PENDIENTE,
    APROBADO,
    DESCARTADO
}
