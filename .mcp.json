{
  "$comment": [
    "Servidores MCP del proyecto AguaVigia CTG.",
    "Este archivo SI se versiona: define los conectores del proyecto.",
    "Los secretos NUNCA van aqui. Se referencian como variables de entorno con la sintaxis ${VAR}",
    "y se definen en el .env local (que esta en .gitignore).",
    "",
    "Para activar un servidor: descomentalo, instala su paquete y define sus variables.",
    "Verifica con: claude mcp list"
  ],

  "mcpServers": {
    "filesystem": {
      "$comment": "Acceso de lectura a la documentacion del proyecto. Util para que el agente navegue docs/ sin abrir archivo por archivo.",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "./docs"
      ]
    },

    "playwright": {
      "$comment": "Maneja el frontend real: capturas en 360 y 1280 px en los dos temas, flujos y recorridos solo con teclado (docs/ingenieria/plan-frontend.md §9).",
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest"]
    },

    "chrome-devtools": {
      "$comment": "Trazas de rendimiento, Lighthouse con 3G simulado y revision de la red y del SSE del frontend.",
      "command": "npx",
      "args": ["-y", "chrome-devtools-mcp@latest"]
    },

    "$comment_git": [
      "NO hay servidor MCP de git aqui a proposito. El paquete npm",
      "@modelcontextprotocol/server-git NO existe (verificado 2026-08-07: npm devuelve E404);",
      "el servidor oficial es de Python (uvx mcp-server-git) y agregaria una dependencia",
      "de Python al entorno del proyecto.",
      "El historial de git ya esta disponible via Bash(git log/diff/show), permitidos en",
      "settings.json. No reintroducir sin verificar que el paquete exista."
    ],

    "$disabled_mongodb": {
      "$comment": "ACTIVAR EN SPRINT 1, cuando exista la base de datos. Permite consultar sectores y cortes directamente. Definir MONGODB_URI en el .env local.",
      "command": "npx",
      "args": ["-y", "mongodb-mcp-server"],
      "env": {
        "MONGODB_URI": "${MONGODB_URI}"
      }
    },

    "$disabled_github": {
      "$comment": "ACTIVAR EN SPRINT 0, tras crear el repositorio. Permite leer issues y PRs para las ceremonias de Scrum. Requiere un token de acceso personal con permiso de solo lectura sobre el repo.",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_PERSONAL_ACCESS_TOKEN}"
      }
    }
  }
}
